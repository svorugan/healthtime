# Doctor API Flow Diagram

## 🔄 Complete API Testing Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                     HEALTHTIME DOCTOR API                        │
│                     Testing Flow Diagram                         │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                    1. PUBLIC ENDPOINTS                           │
│                   (No Authentication)                            │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
        ┌─────────────────────────────────────────┐
        │  GET /api/doctors/surgeons              │
        │  • Returns all approved surgeons        │
        │  • Filter by location (optional)        │
        │  • No auth required                     │
        └─────────────────────────────────────────┘
                              │
                              ▼
        ┌─────────────────────────────────────────┐
        │  GET /api/doctors/:doctor_id            │
        │  • Get specific doctor details          │
        │  • Includes surgeries                   │
        │  • No auth required                     │
        └─────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                  2. DOCTOR REGISTRATION                          │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
        ┌─────────────────────────────────────────┐
        │  POST /api/auth/register/doctor         │
        │  • Email, password, credentials         │
        │  • Status: "pending"                    │
        │  • Returns: user_id                     │
        └─────────────────────────────────────────┘
                              │
                              ▼
                    [Doctor Pending Approval]

┌─────────────────────────────────────────────────────────────────┐
│                   3. ADMIN APPROVAL FLOW                         │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
        ┌─────────────────────────────────────────┐
        │  POST /api/auth/login                   │
        │  • Role: "admin"                        │
        │  • Returns: access_token                │
        │  • Token auto-saved                     │
        └─────────────────────────────────────────┘
                              │
                              ▼
        ┌─────────────────────────────────────────┐
        │  GET /api/doctors/admin/pending         │
        │  • Requires: Admin token                │
        │  • Returns: List of pending doctors     │
        └─────────────────────────────────────────┘
                              │
                              ▼
        ┌─────────────────────────────────────────┐
        │  PATCH /api/doctors/admin/:id/approve   │
        │  • Requires: Admin token                │
        │  • Status changes to "approved"         │
        │  • Doctor can now login                 │
        └─────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                  4. DOCTOR LOGIN & OPERATIONS                    │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
        ┌─────────────────────────────────────────┐
        │  POST /api/auth/login                   │
        │  • Role: "doctor"                       │
        │  • Returns: access_token, user_id       │
        │  • Both auto-saved                      │
        └─────────────────────────────────────────┘
                              │
                ┌─────────────┴─────────────┐
                │                           │
                ▼                           ▼
    ┌───────────────────────┐   ┌───────────────────────┐
    │  Profile Management   │   │  Data Retrieval       │
    └───────────────────────┘   └───────────────────────┘
                │                           │
                ▼                           ▼
    ┌───────────────────────┐   ┌───────────────────────┐
    │ PUT /doctors/:id      │   │ GET /doctors/:id/     │
    │ • Update profile      │   │     bookings          │
    │ • Update fees         │   │ • View appointments   │
    │ • Update bio          │   │                       │
    └───────────────────────┘   └───────────────────────┘
                │                           │
                ▼                           ▼
    ┌───────────────────────┐   ┌───────────────────────┐
    │ PUT /doctors/:id/     │   │ GET /doctors/:id/     │
    │     availability      │   │     schedule          │
    │ • Online consult      │   │ • Date range filter   │
    │ • In-person           │   │                       │
    │ • Emergency           │   └───────────────────────┘
    └───────────────────────┘               │
                │                           ▼
                ▼                   ┌───────────────────────┐
    ┌───────────────────────┐       │ GET /doctors/:id/     │
    │ PUT /doctors/:id/     │       │     patients          │
    │     surgeries         │       │ • Unique patients     │
    │ • Update surgery list │       │                       │
    └───────────────────────┘       └───────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                    5. FILE UPLOAD FLOW                           │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
        ┌─────────────────────────────────────────┐
        │  POST /api/doctors/upload/verification  │
        │  • Form-data (not JSON)                 │
        │  • File + doctor_id + document_type     │
        │  • Uploads to S3                        │
        │  • Returns: file_url                    │
        └─────────────────────────────────────────┘
                              │
                              ▼
                    [Document Stored in S3]
                              │
                              ▼
                [Doctor record updated with URL]

┌─────────────────────────────────────────────────────────────────┐
│                    6. VERIFICATION FLOW                          │
└─────────────────────────────────────────────────────────────────┘

    Doctor Registration → Pending Status → Admin Review
            │                                    │
            ▼                                    ▼
    Upload Documents                    View Pending Doctors
            │                                    │
            ▼                                    ▼
    Medical Degree                         Verify Credentials
    Council Certificate                          │
    Photo ID                                     ▼
    Insurance                              Approve/Reject
            │                                    │
            └────────────────┬───────────────────┘
                             ▼
                    [Doctor Approved]
                             │
                             ▼
                    Visible in Public API
                             │
                             ▼
                    Can Accept Bookings
```

---

## 🔐 Authentication Flow

```
┌──────────────┐
│   Request    │
└──────┬───────┘
       │
       ▼
┌──────────────────────────────┐
│  Has Authorization Header?   │
└──────┬───────────────────────┘
       │
       ├─── NO ──→ [401 Unauthorized]
       │
       └─── YES
            │
            ▼
    ┌──────────────────┐
    │  Valid Token?    │
    └──────┬───────────┘
           │
           ├─── NO ──→ [401 Unauthorized]
           │
           └─── YES
                │
                ▼
        ┌──────────────────┐
        │  Correct Role?   │
        └──────┬───────────┘
               │
               ├─── NO ──→ [403 Forbidden]
               │
               └─── YES
                    │
                    ▼
            [Process Request]
                    │
                    ▼
            [Return Response]
```

---

## 📊 Data Flow Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                        CLIENT (Postman)                          │
└─────────────────────────────────────────────────────────────────┘
                              │
                              │ HTTP Request
                              │ (JSON/Form-data)
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                      EXPRESS SERVER                              │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │  Middleware Stack                                          │ │
│  │  • CORS                                                    │ │
│  │  • Body Parser                                             │ │
│  │  • Authentication                                          │ │
│  │  • Authorization                                           │ │
│  └────────────────────────────────────────────────────────────┘ │
│                              │                                   │
│                              ▼                                   │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │  Route Handler (doctors.js)                               │ │
│  │  • Validate request                                        │ │
│  │  • Process business logic                                  │ │
│  │  • Call database                                           │ │
│  └────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                      SEQUELIZE ORM                               │
│  • Build SQL queries                                             │
│  • Handle relationships                                          │
│  • Validate data                                                 │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                    POSTGRESQL DATABASE                           │
│  ┌────────────┐  ┌────────────┐  ┌────────────┐               │
│  │  doctors   │  │  bookings  │  │  surgeries │               │
│  └────────────┘  └────────────┘  └────────────┘               │
└─────────────────────────────────────────────────────────────────┘
                              │
                              │ Query Results
                              ▼
                    [Response to Client]
```

---

## 🎯 Request/Response Flow

### Example: Update Doctor Profile

```
1. CLIENT REQUEST
   ═══════════════
   PUT /api/doctors/abc-123-def
   Authorization: Bearer eyJhbGc...
   Content-Type: application/json
   
   {
     "bio": "Updated bio",
     "consultation_fee": 2000
   }

2. MIDDLEWARE PROCESSING
   ═══════════════════════
   ┌─→ CORS Check ✓
   ├─→ Parse JSON ✓
   ├─→ Authenticate Token ✓
   ├─→ Verify Role (doctor) ✓
   └─→ Check doctor_id matches ✓

3. ROUTE HANDLER
   ═══════════════
   ┌─→ Find doctor by ID
   ├─→ Validate update data
   ├─→ Remove protected fields
   └─→ Update database

4. DATABASE OPERATION
   ═══════════════════
   UPDATE doctors
   SET bio = 'Updated bio',
       consultation_fee = 2000,
       updated_at = NOW()
   WHERE id = 'abc-123-def'

5. SERVER RESPONSE
   ════════════════
   200 OK
   Content-Type: application/json
   
   {
     "message": "Doctor profile updated",
     "doctor": {
       "id": "abc-123-def",
       "bio": "Updated bio",
       "consultation_fee": 2000,
       ...
     }
   }
```

---

## 🔄 Status Transitions

```
Doctor Lifecycle
════════════════

    [Registration]
         │
         ▼
    ┌─────────┐
    │ PENDING │ ◄─── Initial status
    └────┬────┘
         │
         ├─────────────┬─────────────┐
         │             │             │
         ▼             ▼             ▼
    ┌─────────┐   ┌─────────┐   ┌─────────┐
    │APPROVED │   │REJECTED │   │ PENDING │
    └────┬────┘   └─────────┘   └─────────┘
         │             │
         │             └──→ [End]
         │
         ▼
    [Active Doctor]
         │
         ├─→ Can update profile
         ├─→ Can manage availability
         ├─→ Can view bookings
         ├─→ Visible in public API
         └─→ Can accept patients
```

---

## 🎨 Collection Structure

```
Doctor_API_Collection
│
├── 📁 Authentication
│   ├── 🔐 Login as Admin
│   ├── 🔐 Login as Doctor
│   └── 📝 Register Doctor
│
├── 📁 Public Endpoints
│   ├── 👥 Get All Approved Surgeons
│   ├── 📍 Get Surgeons by Location
│   ├── 👤 Get Doctor by ID
│   └── 🏥 Get Doctor's Surgeries
│
├── 📁 Doctor Protected Endpoints
│   ├── ✏️ Update Doctor Profile
│   ├── 📅 Update Doctor Availability
│   ├── 🔧 Update Doctor Surgery Types
│   ├── 📋 Get Doctor's Bookings
│   ├── 🗓️ Get Doctor's Schedule
│   └── 👥 Get Doctor's Patients
│
├── 📁 Admin Endpoints
│   ├── ⏳ Get Pending Doctors
│   ├── ✅ Approve Doctor
│   └── ❌ Reject Doctor
│
└── 📁 File Upload
    └── 📄 Upload Doctor Verification Document
```

---

## 🔍 Testing Paths

### Path 1: Quick Public Test (No Auth)
```
Start → Get All Surgeons → Get Doctor by ID → Done
Time: 1 minute
```

### Path 2: Full Registration Flow
```
Start → Register Doctor → Login Admin → Approve Doctor 
      → Login Doctor → Update Profile → Verify → Done
Time: 5 minutes
```

### Path 3: Doctor Management
```
Start → Login Doctor → Update Profile → Update Availability 
      → Update Surgeries → View Bookings → Done
Time: 3 minutes
```

### Path 4: Admin Workflow
```
Start → Login Admin → Get Pending → Review → Approve/Reject 
      → Verify in Public API → Done
Time: 2 minutes
```

---

## 📈 Success Metrics

```
✅ All Public Endpoints Working
   ├─ Get surgeons (200 OK)
   ├─ Filter by location (200 OK)
   └─ Get doctor details (200 OK)

✅ Authentication Working
   ├─ Admin login (200 OK + token)
   ├─ Doctor login (200 OK + token)
   └─ Token auto-saved

✅ Protected Endpoints Working
   ├─ Profile updates (200 OK)
   ├─ Availability updates (200 OK)
   └─ Data retrieval (200 OK)

✅ Admin Functions Working
   ├─ View pending (200 OK)
   ├─ Approve doctor (200 OK)
   └─ Status updated in DB

✅ File Upload Working
   └─ Document uploaded (200 OK + S3 URL)
```

---

## 🎯 Key Takeaways

1. **Public endpoints** don't require authentication
2. **Tokens auto-save** after login requests
3. **Doctor operations** require matching doctor_id
4. **Admin operations** require admin role
5. **File uploads** use form-data, not JSON
6. **Status flow**: pending → approved → active

---

**Use this diagram alongside the Postman collection for visual reference!**
